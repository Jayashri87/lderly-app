# @lderly/location

Target package for reusable location, geocoding, routing, and ETA contracts.

Current runtime implementation remains in:

- `server/locationProvider.ts`
- `components/LiveMapClient.tsx`
- `components/GoogleAddressSelector.tsx`
