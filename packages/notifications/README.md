# @lderly/notifications

Target package for notification contracts and delivery adapters.

Current runtime implementation remains in:

- `server/communicationProvider.ts`
- `server/pushProvider.ts`
- `server/notificationRetryProvider.ts`
- `services/notificationService.ts`
