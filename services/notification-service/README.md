# notification-service

Target service package for push, WhatsApp/SMS provider routing, retries, and lifecycle notifications.

Current runtime implementation:

- `server/communicationProvider.ts`
- `server/pushProvider.ts`
- `server/notificationRetryProvider.ts`
- `app/api/notifications/*`
