# matching-service

Target service package for caregiver scoring, availability, area broadcast, and backup recommendations.

Current runtime implementation:

- `server/trustedBooking.ts`
- `server/caregiverIntelligence.ts`
