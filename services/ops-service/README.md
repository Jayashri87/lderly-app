# ops-service

Target service package for operations command center, recovery workflows, runbooks, backup manifests, and audit retention.

Current runtime implementation:

- `server/opsRecoveryProvider.ts`
- `server/opsRunbookProvider.ts`
- `server/opsMaintenanceProvider.ts`
- `server/emergencyCommandCenter.ts`
