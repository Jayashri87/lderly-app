# booking-service

Target service package for booking orchestration, state transitions, idempotency, and lifecycle policies.

Current runtime implementation:

- `server/trustedBooking.ts`
- `services/bookingService.ts`
- `app/api/bookings/*`
