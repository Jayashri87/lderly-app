# tracking-service

Target service package for live location, ETA, route state, and tracking recovery.

Current runtime implementation:

- `server/locationProvider.ts`
- `app/api/locations/*`
- `app/api/caretaker/location`
