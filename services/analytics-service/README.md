# analytics-service

Target service package for funnel analytics, operational KPIs, ML-ready event history, and reporting.

Current runtime implementation:

- `server/observability.ts`
- `server/aiIntelligenceProvider.ts`
- `app/api/analytics/events`
- `app/api/ops/kpis`
